name: Android Factory Reset Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    # Use official Setup-Gradle action. 
    # It handles wrapper validation, distribution download, and caching.
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.0'
        
    - name: Build Debug APK
      run: gradle assembleDebug --stacktrace --info
